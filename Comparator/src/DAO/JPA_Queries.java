package DAO;


public class JPA_Queries {

/*	
	EntityManager em = new EntityManager();
	
	// --- Criteria Query
	CriteriaBuilder cb = em.getCriteriaBuilder();
	 
	// Query for a single data element.
	CriteriaQuery cq = cb.createQuery();
	Root e = cq.from(User.class);
	Query query = em.createQuery(cq);
	User result3 = (User)query.getSingleResult();
	
//	--- JPQL Query
	Query query2 = em.createQuery("FROM User e WHERE e.id = :id");
	query2.setParameter("id", 1);
	User result2 = (User)query2.getSingleResult();
	
*/
}
